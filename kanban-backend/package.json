{
  "name": "kanban-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Local dev backend (json-server) for the Kanban homework. GET / POST / PATCH / DELETE.",
  "scripts": {
    "start": "json-server --watch db.json --port 4000 --delay 400",
    "start:fast": "json-server --watch db.json --port 4000"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}
